import React from "react";
import"./Card.css";
const Card = ({gunData}) => {
    //console.log(props.gunData);
    const {name} =gunData;
      //console.log(name);
  
    return(

        <div className="card">
            <div>
                
            </div>
            <h1>Card</h1>
        </div>
    )
};

export default Card;
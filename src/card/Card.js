import React from "react";
import"./Card.css";
const Card = ({gunData}) => {
    const {name} =gunData;
  
    return(

        <div className="card">
            <div>

            </div>
            <h1>Card</h1>
        </div>
    )
};

export default Card;